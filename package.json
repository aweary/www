{
  "name": "brandondailme",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "open app.html",
    "build": "node build",
    "deploy": "yarn build && now --static"
  },
  "dependencies": {
    "html-minifier": "^3.5.1"
  }
}
